-name: To print some message on the console
hosts: all
tasks:
  -name: printing message on the console
   debug:
    -msg: System {{ inventory_hostname }} has gateway {{ ansible_default_ipv4.gateway }}